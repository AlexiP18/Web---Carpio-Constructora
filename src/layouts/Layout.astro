<!doctype html>
<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <meta name="description" content="Constructora Carpio - Transformamos sueños en estructuras duraderas. Expertos en construcción industrial, centros logísticos y más." />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>Constructora Carpio - Transformamos sueños en estructuras duraderas</title>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Heebo:wght@400;500;600&family=Urbanist:wght@500&display=swap" rel="stylesheet">
        <!-- Netlify Identity Widget -->
        <script src="https://identity.netlify.com/v1/netlify-identity-widget.js" is:inline></script>
        <style is:global>
            html,
            body {
                margin: 0;
                width: 100%;
                height: 100%;
                font-family: 'Heebo', sans-serif;
                scroll-behavior: smooth;
            }
        </style>
    </head>
    <body>
        <slot />
        {import.meta.env.PROD && (
            <script is:inline>
                if (window.netlifyIdentity) {
                    window.netlifyIdentity.on("init", user => {
                        if (!user) {
                            window.netlifyIdentity.on("login", () => {
                                document.location.href = "/admin/";
                            });
                        }
                    });
                }
            </script>
        )}
    </body>
</html>