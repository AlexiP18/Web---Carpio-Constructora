---
import { Icon } from "astro-icon/components";
import Button from '../ui/Button.astro';

---

<section class="relative py-20 lg:py-32 bg-gradient-to-br from-slate-900 via-gray-900 to-slate-900 overflow-hidden">
  <!-- Background Image with Overlay -->
  <div class="absolute inset-0">
    <img 
      src="/images/header-50.png" 
      alt="Contacto Constructora Carpio"
      class="w-full h-full object-cover opacity-30"
    />
    <div class="absolute inset-0 bg-gradient-to-br from-slate-900/80 via-gray-900/60 to-slate-900/80"></div>
  </div>

  <!-- Background decorations -->
  <div class="absolute inset-0">
    <div class="absolute top-20 left-20 w-40 h-40 bg-primary-green/20 rounded-full blur-3xl"></div>
    <div class="absolute bottom-20 right-20 w-32 h-32 bg-blue-500/20 rounded-full blur-2xl"></div>
    <div class="absolute top-1/2 right-10 w-24 h-24 bg-purple-500/15 rounded-full blur-xl"></div>
  </div>

  <div class="max-w-7xl mx-auto px-6 relative z-10">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <!-- Content Side -->
      <div class="space-y-8" data-aos="fade-right">
        <!-- Badge -->
        <div class="inline-flex items-center gap-2 bg-white/10 backdrop-blur-sm border border-white/20 rounded-full px-4 py-2">
          <Icon name="ph:chat-circle-text-fill" class="w-5 h-5 text-primary-green" />
          <span class="text-white font-semibold text-sm">Contacto Directo</span>
        </div>
        
        <!-- Main Heading -->
        <div class="space-y-6">
          <h1 class="text-5xl lg:text-7xl font-bold text-white leading-tight">
            Hablemos de Tu <span class="bg-gradient-to-r from-primary-green to-emerald-400 bg-clip-text text-transparent">Próximo Proyecto</span>
          </h1>
          
          <p class="text-xl text-gray-300 leading-relaxed max-w-lg">
            Estamos listos para convertir tus ideas en realidad. Contacta con nuestro equipo de expertos y comencemos a construir juntos.
          </p>
        </div>

        <!-- Stats Cards -->
        <div class="grid grid-cols-3 gap-4" data-aos="fade-up" data-aos-delay="200">
          <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-4 border border-white/10 text-center">
            <div class="text-2xl font-bold text-white">150+</div>
            <div class="text-sm text-gray-300">Proyectos</div>
          </div>
          <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-4 border border-white/10 text-center">
            <div class="text-2xl font-bold text-white">24h</div>
            <div class="text-sm text-gray-300">Respuesta</div>
          </div>
          <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-4 border border-white/10 text-center">
            <div class="text-2xl font-bold text-white">15+</div>
            <div class="text-sm text-gray-300">Años</div>
          </div>
        </div>

        <!-- Action Buttons -->
        <div class="flex flex-col sm:flex-row gap-4" data-aos="fade-up" data-aos-delay="300">
          <Button variant="success" href="#form" class="group">
            <Icon name="ph:rocket-launch-fill" class="w-5 h-5 group-hover:animate-bounce" />
            Solicitar Cotización
          </Button>
          <Button variant="outline" href="#info" class="group">
            <Icon name="ph:phone-fill" class="w-5 h-5 group-hover:scale-110 transition-transform" />
            Llamar Ahora
          </Button>
        </div>

        <!-- Quick Contact -->
        <div class="flex items-center gap-6 pt-4" data-aos="fade-up" data-aos-delay="400">
          <div class="flex items-center gap-3 text-white">
            <div class="w-12 h-12 bg-primary-green/20 backdrop-blur-sm rounded-xl flex items-center justify-center">
              <Icon name="ph:whatsapp-logo-fill" class="w-6 h-6 text-primary-green" />
            </div>
            <div>
              <div class="text-sm text-gray-300">WhatsApp</div>
              <div class="font-semibold">095 632 4598</div>
            </div>
          </div>
          
          <div class="w-px h-12 bg-white/20"></div>
          
          <div class="flex items-center gap-3 text-white">
            <div class="w-12 h-12 bg-blue-500/20 backdrop-blur-sm rounded-xl flex items-center justify-center">
              <Icon name="ph:envelope-fill" class="w-6 h-6 text-blue-400" />
            </div>
            <div>
              <div class="text-sm text-gray-300">Email</div>
              <div class="font-semibold">hola@carpio.com</div>
            </div>
          </div>
        </div>
      </div>

      <!-- Visual Side -->
      <div class="relative" data-aos="fade-left" data-aos-delay="200">
        <!-- Main Contact Card -->
        <div class="bg-white/10 backdrop-blur-sm rounded-3xl p-8 border border-white/20 relative">
          <!-- Floating Elements -->
          <div class="absolute -top-4 -right-4 w-16 h-16 bg-primary-green/30 rounded-2xl rotate-12 blur-sm"></div>
          <div class="absolute -bottom-4 -left-4 w-12 h-12 bg-blue-500/30 rounded-xl -rotate-12 blur-sm"></div>
          
          <div class="space-y-6">
            <div class="text-center">
              <div class="w-20 h-20 bg-gradient-to-br from-primary-green to-emerald-500 rounded-2xl flex items-center justify-center mx-auto mb-4 shadow-lg shadow-primary-green/30">
                <Icon name="ph:buildings-fill" class="w-10 h-10 text-white" />
              </div>
              <h3 class="text-xl font-bold text-white mb-2">Constructora Carpio</h3>
              <p class="text-gray-300 text-sm">Construyendo sueños desde 2009</p>
            </div>

            <!-- Quick Info Grid -->
            <div class="grid grid-cols-2 gap-4 pt-4">
              <div class="text-center">
                <div class="text-2xl font-bold text-primary-green">24/7</div>
                <div class="text-xs text-gray-300">Atención</div>
              </div>
              <div class="text-center">
                <div class="text-2xl font-bold text-blue-400">100%</div>
                <div class="text-xs text-gray-300">Garantía</div>
              </div>
              <div class="text-center">
                <div class="text-2xl font-bold text-purple-400">15min</div>
                <div class="text-xs text-gray-300">Respuesta</div>
              </div>
              <div class="text-center">
                <div class="text-2xl font-bold text-orange-400">Gratis</div>
                <div class="text-xs text-gray-300">Cotización</div>
              </div>
            </div>
          </div>
        </div>

        <!-- Floating Action Cards -->
        <div class="absolute -top-8 left-8 bg-white/10 backdrop-blur-sm rounded-2xl p-4 border border-white/20 animate-float">
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 bg-green-500/30 rounded-xl flex items-center justify-center">
              <Icon name="ph:check-circle-fill" class="w-5 h-5 text-green-400" />
            </div>
            <div class="text-white">
              <div class="text-sm font-semibold">Presupuesto Gratuito</div>
              <div class="text-xs text-gray-300">En menos de 24h</div>
            </div>
          </div>
        </div>

        <div class="absolute -bottom-8 right-8 bg-white/10 backdrop-blur-sm rounded-2xl p-4 border border-white/20 animate-float-delayed">
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 bg-blue-500/30 rounded-xl flex items-center justify-center">
              <Icon name="ph:headset-fill" class="w-5 h-5 text-blue-400" />
            </div>
            <div class="text-white">
              <div class="text-sm font-semibold">Asesoría Personalizada</div>
              <div class="text-xs text-gray-300">Sin compromiso</div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Scroll Indicator -->
    <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2" data-aos="fade-up" data-aos-delay="600">
      <div class="flex flex-col items-center gap-2 text-white/60">
        <span class="text-sm font-medium">Desliza para más información</span>
        <div class="w-6 h-10 border-2 border-white/30 rounded-full flex justify-center">
          <div class="w-1 h-3 bg-white/60 rounded-full animate-bounce mt-2"></div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
  }
  
  @keyframes float-delayed {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-15px); }
  }
  
  .animate-float {
    animation: float 3s ease-in-out infinite;
  }
  
  .animate-float-delayed {
    animation: float-delayed 3s ease-in-out infinite 1.5s;
  }
</style>