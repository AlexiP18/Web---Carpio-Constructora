---
import { Icon } from "astro-icon/components";
import Button from '../ui/Button.astro';
---

<section class="relative py-20 lg:py-32 bg-gradient-to-br from-slate-900 via-gray-900 to-slate-900 overflow-hidden">
  <!-- Background Image with Overlay -->
  <div class="absolute inset-0">
    <img 
      src="/images/header-50.png" 
      alt="Proyectos Constructora Carpio"
      class="w-full h-full object-cover opacity-30"
    />
    <div class="absolute inset-0 bg-gradient-to-br from-slate-900/70 via-gray-900/50 to-slate-900/70"></div>
  </div>

  <!-- Background decorations -->
  <div class="absolute inset-0">
    <div class="absolute top-20 right-20 w-40 h-40 bg-primary-green/10 rounded-full blur-3xl"></div>
    <div class="absolute bottom-20 left-20 w-32 h-32 bg-blue-500/10 rounded-full blur-2xl"></div>
    <div class="absolute top-1/2 left-1/4 w-24 h-24 bg-purple-500/10 rounded-full blur-xl"></div>
  </div>

  <div class="max-w-7xl mx-auto px-6 relative z-10">
    <div class="max-w-4xl">
      <!-- Badge -->
      <div class="inline-flex items-center gap-2 bg-primary-green/10 backdrop-blur-sm border border-primary-green/20 rounded-full px-4 py-2 mb-8" data-aos="fade-up">
        <Icon name="ph:buildings-fill" class="w-5 h-5 text-primary-green" />
        <span class="text-primary-green font-semibold text-sm">Proyectos Destacados</span>
      </div>

      <!-- Main Content -->
      <div class="space-y-8">
        <h1 class="text-5xl lg:text-7xl font-bold text-white leading-tight" data-aos="fade-up" data-aos-delay="100">
          Nuestros Proyectos <span class="bg-gradient-to-r from-primary-green to-emerald-400 bg-clip-text text-transparent">Destacados</span>
        </h1>
        
        <p class="text-xl lg:text-2xl text-gray-300 leading-relaxed max-w-3xl" data-aos="fade-up" data-aos-delay="200">
          Descubre la calidad, creatividad e innovación en cada uno de nuestros proyectos de construcción, 
          donde transformamos ideas en realidades excepcionales.
        </p>

        <!-- Stats Cards -->
        <div class="grid lg:grid-cols-3 gap-6 max-w-4xl pt-8" data-aos="fade-up" data-aos-delay="300">
          <div class="bg-white/10 backdrop-blur-sm border border-white/20 rounded-2xl p-6 text-center">
            <div class="text-3xl font-bold text-primary-green mb-2">150+</div>
            <div class="text-gray-300">Proyectos Completados</div>
          </div>
          <div class="bg-white/10 backdrop-blur-sm border border-white/20 rounded-2xl p-6 text-center">
            <div class="text-3xl font-bold text-primary-green mb-2">15+</div>
            <div class="text-gray-300">Años de Experiencia</div>
          </div>
          <div class="bg-white/10 backdrop-blur-sm border border-white/20 rounded-2xl p-6 text-center">
            <div class="text-3xl font-bold text-primary-green mb-2">100%</div>
            <div class="text-gray-300">Clientes Satisfechos</div>
          </div>
        </div>

        <!-- Action Buttons -->
        <div class="flex flex-col sm:flex-row gap-4 pt-4" data-aos="fade-up" data-aos-delay="400">
          <Button variant="success" href="#portfolio" class="group">
            <Icon name="ph:eye-fill" class="w-5 h-5 group-hover:scale-110 transition-transform" />
            Ver Portafolio
          </Button>
          <Button variant="outline" href="/contacto">
            <Icon name="ph:chat-circle-text-fill" class="w-5 h-5" />
            Solicitar Información
          </Button>
        </div>
      </div>
    </div>
  </div>

  <!-- Floating Elements -->
  <div class="absolute bottom-10 right-10 w-20 h-20 bg-gradient-to-br from-primary-green/20 to-emerald-500/20 rounded-2xl flex items-center justify-center backdrop-blur-sm border border-white/10" data-aos="zoom-in" data-aos-delay="500">
    <Icon name="ph:hammer-fill" class="w-10 h-10 text-primary-green" />
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce" data-aos="fade-up" data-aos-delay="600">
    <Icon name="ph:arrow-down" class="w-6 h-6 text-white/60" />
  </div>
</section>
