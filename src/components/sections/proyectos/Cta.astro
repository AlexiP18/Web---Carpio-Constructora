---
import { Icon } from "astro-icon/components";
import Button from '../../ui/Button.astro';
---

<section class="py-20 bg-gradient-to-br from-white via-gray-50 to-slate-100 relative overflow-hidden" data-aos="fade-up">
  <!-- Background decorations -->
  <div class="absolute inset-0">
    <div class="absolute top-20 left-20 w-40 h-40 bg-primary-green/5 rounded-full blur-3xl"></div>
    <div class="absolute bottom-20 right-20 w-32 h-32 bg-blue-500/5 rounded-full blur-2xl"></div>
  </div>

  <div class="max-w-7xl mx-auto px-6 relative z-10">
    <div class="bg-gradient-to-br from-gray-900 via-slate-800 to-gray-900 rounded-3xl p-12 lg:p-16 text-white relative overflow-hidden shadow-2xl">
      <!-- Background decorations -->
      <div class="absolute inset-0">
        <div class="absolute top-0 left-0 w-full h-full bg-gradient-to-br from-primary-green/10 via-emerald-500/5 to-transparent"></div>
        <div class="absolute -top-20 -right-20 w-40 h-40 bg-primary-green/10 rounded-full blur-3xl"></div>
        <div class="absolute -bottom-10 -left-10 w-32 h-32 bg-emerald-500/10 rounded-full blur-2xl"></div>
      </div>

      <div class="relative z-10">
        <div class="flex flex-col lg:flex-row justify-between items-start gap-12 lg:gap-20">
          <!-- Left side - Content -->
          <div class="flex-1 text-center lg:text-left" data-aos="fade-right" data-aos-delay="100">
            <div class="inline-flex items-center gap-2 bg-primary-green/20 backdrop-blur-sm border border-primary-green/30 rounded-full px-4 py-2 mb-6">
              <Icon name="ph:phone-fill" class="w-5 h-5 text-primary-green" />
              <span class="text-primary-green font-semibold text-sm">Contáctanos</span>
            </div>
            
            <h2 class="text-4xl lg:text-5xl xl:text-6xl font-bold leading-tight mb-6">
              ¡Contáctanos para más <span class="bg-gradient-to-r from-primary-green to-emerald-400 bg-clip-text text-transparent">información!</span>
            </h2>
          </div>
          
          <!-- Right side - Content and buttons -->
          <div class="flex-1 text-center lg:text-left" data-aos="fade-left" data-aos-delay="200">
            <p class="text-xl text-gray-300 leading-relaxed mb-8 max-w-2xl">
              Estamos aquí para ayudarte a hacer realidad tus proyectos. No dudes en
              ponerte en contacto con nosotros para obtener más detalles sobre
              nuestros servicios.
            </p>
            
            <div class="flex flex-col sm:flex-row gap-4" data-aos="fade-up" data-aos-delay="300">
              <Button
                href="/contacto"
                variant="primary"
                size="lg"
                icon="ph:arrow-right"
                class="shadow-lg hover:shadow-xl"
              >
                Solicitar Cotización
              </Button>
              
              <Button
                href="tel:+593956324598"
                variant="outline-dark"
                size="lg"
                icon="ph:phone-fill"
                class="shadow-lg hover:shadow-xl"
              >
                Llamar Ahora
              </Button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
