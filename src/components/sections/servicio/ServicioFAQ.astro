---
import { Icon } from "astro-icon/components";
import Button from '../ui/Button.astro';

export interface Props {
  tagline: string;
  title: string;
  description: string;
  faqs: Array<{
    question: string;
    answer: string;
  }>;
}

const { tagline, title, description, faqs } = Astro.props;
---

<section class="py-20 bg-gradient-to-br from-white via-gray-50 to-white relative overflow-hidden" id="faq">
  <!-- Background decorations -->
  <div class="absolute inset-0">
    <div class="absolute top-20 right-20 w-40 h-40 bg-primary-green/5 rounded-full blur-3xl"></div>
    <div class="absolute bottom-20 left-20 w-32 h-32 bg-blue-500/5 rounded-full blur-2xl"></div>
  </div>

  <div class="max-w-6xl mx-auto px-6 relative z-10">
    <!-- Section Header -->
    <div class="text-center max-w-4xl mx-auto mb-16" data-aos="fade-up">
      <div class="inline-flex items-center gap-2 bg-primary-green/10 backdrop-blur-sm border border-primary-green/20 rounded-full px-4 py-2 mb-6">
        <Icon name="ph:question-fill" class="w-5 h-5 text-primary-green" />
        <span class="text-primary-green font-semibold text-sm uppercase tracking-wider">{tagline}</span>
      </div>
      
      <h2 class="text-4xl lg:text-6xl font-bold text-gray-900 leading-tight mb-6">
        {title}
      </h2>
      
      <p class="text-xl text-gray-600 leading-relaxed">
        {description}
      </p>
    </div>

    <div class="grid lg:grid-cols-2 gap-12 items-start">
      <!-- FAQ Items -->
      <div class="space-y-4">
        {faqs.map((faq, index) => (
          <div class="group bg-white rounded-2xl shadow-lg border border-gray-100 hover:border-primary-green/20 transition-all duration-300 overflow-hidden" data-aos="fade-up" data-aos-delay={100 + (index * 75)}>
            <details class="faq-item">
              <summary class="cursor-pointer flex items-center justify-between p-6 hover:bg-gray-50 transition-colors list-none group">
                <h3 class="font-bold text-gray-900 text-lg group-hover:text-primary-green transition-colors pr-4">
                  {faq.question}
                </h3>
                <div class="w-10 h-10 bg-primary-green/10 rounded-xl flex items-center justify-center flex-shrink-0 group-hover:bg-primary-green/20 transition-colors">
                  <Icon 
                    name="ph:plus-bold" 
                    class="w-5 h-5 text-primary-green transition-transform duration-300 group-open:rotate-45" 
                  />
                </div>
              </summary>
              
              <div class="border-t border-gray-100 px-6 pb-6">
                <div class="pt-4 text-gray-600 leading-relaxed">
                  {faq.answer}
                </div>
              </div>
            </details>
          </div>
        ))}
      </div>

      <!-- Side Content -->
      <div class="lg:sticky lg:top-8 space-y-8">
        <!-- Support Card -->
        <div class="bg-gradient-to-br from-slate-900 to-slate-800 rounded-3xl p-8 relative overflow-hidden" data-aos="fade-left" data-aos-delay="200">
          <!-- Background decoration -->
          <div class="absolute top-0 right-0 w-24 h-24 bg-primary-green/10 rounded-full blur-2xl"></div>
          
          <div class="relative z-10 space-y-6">
            <div class="w-16 h-16 bg-gradient-to-br from-primary-green to-emerald-500 rounded-2xl flex items-center justify-center">
              <Icon name="ph:headset-fill" class="w-8 h-8 text-white" />
            </div>
            
            <div class="space-y-4">
              <h3 class="text-2xl font-bold text-white">¿Tienes más preguntas?</h3>
              <p class="text-gray-300 leading-relaxed">
                Nuestro equipo de expertos está disponible para resolver todas tus dudas 
                y brindarte la asesoría personalizada que necesitas.
              </p>
            </div>
            
            <div class="space-y-3">
              <Button variant="success" href="/contacto" class="w-full" iconPosition="left" icon="ph:chat-circle-text-fill">
                Contactar Especialista
              </Button>
              <Button variant="outline" href="tel:+50612345678" class="w-full" iconPosition="left" icon="ph:phone-fill">
                Llamar Ahora
              </Button>
            </div>
          </div>
        </div>

        <!-- Quick Stats -->
        <!-- <div class="bg-white rounded-3xl p-8 shadow-lg border border-gray-100" data-aos="fade-left" data-aos-delay="400">
          <h4 class="text-xl font-bold text-gray-900 mb-6">En números</h4>
          
          <div class="space-y-4">
            <div class="flex items-center gap-4">
              <div class="w-12 h-12 bg-primary-green/10 rounded-xl flex items-center justify-center">
                <Icon name="ph:clock-fill" class="w-6 h-6 text-primary-green" />
              </div>
              <div>
                <div class="font-bold text-gray-900">&lt; 24h</div>
                <div class="text-gray-600 text-sm">Tiempo de respuesta</div>
              </div>
            </div>
            
            <div class="flex items-center gap-4">
              <div class="w-12 h-12 bg-blue-500/10 rounded-xl flex items-center justify-center">
                <Icon name="ph:users-fill" class="w-6 h-6 text-blue-500" />
              </div>
              <div>
                <div class="font-bold text-gray-900">150+</div>
                <div class="text-gray-600 text-sm">Clientes satisfechos</div>
              </div>
            </div>
            
            <div class="flex items-center gap-4">
              <div class="w-12 h-12 bg-purple-500/10 rounded-xl flex items-center justify-center">
                <Icon name="ph:star-fill" class="w-6 h-6 text-purple-500" />
              </div>
              <div>
                <div class="font-bold text-gray-900">4.9/5</div>
                <div class="text-gray-600 text-sm">Calificación promedio</div>
              </div>
            </div>
          </div>
        </div> -->
      </div>
    </div>
  </div>
</section>

<script>
  // Enhanced FAQ functionality with smooth animations
  document.addEventListener('DOMContentLoaded', function() {
    const faqItems = document.querySelectorAll('.faq-item');
    
    faqItems.forEach((item) => {
      const summary = item.querySelector('summary');
      
      item.addEventListener('toggle', function(this: HTMLDetailsElement) {
        if (this.open) {
          // Close other open items with animation
          faqItems.forEach((otherItem) => {
            const otherDetails = otherItem as HTMLDetailsElement;
            if (otherDetails !== this && otherDetails.open) {
              otherDetails.open = false;
            }
          });
          
          // Add open state styling
          this.parentElement?.classList.add('ring-2', 'ring-primary-green/20');
        } else {
          // Remove open state styling
          this.parentElement?.classList.remove('ring-2', 'ring-primary-green/20');
        }
      });
    });
  });
</script>
