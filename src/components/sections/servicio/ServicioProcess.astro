---
import { Icon } from "astro-icon/components";
import Button from '../../ui/Button.astro';

export interface Props {
  tagline: string;
  title: string;
  description: string;
  steps: Array<{
    number: string;
    title: string;
    description: string;
    icon?: string;
  }>;
}

const { tagline, title, description, steps } = Astro.props;
---

<section class="py-20 bg-gradient-to-br from-gray-50 via-white to-gray-50 relative overflow-hidden" id="process">
  <!-- Decorative background elements -->
  <div class="absolute top-0 left-0 w-72 h-72 bg-brand-primary/5 rounded-full blur-3xl"></div>
  <div class="absolute bottom-0 right-0 w-96 h-96 bg-brand-secondary/5 rounded-full blur-3xl"></div>

  <div class="max-w-7xl mx-auto px-6 relative z-10">
    <!-- Section Header -->
    <div class="text-center max-w-4xl mx-auto mb-16" data-aos="fade-up">
      <div class="inline-flex items-center gap-2 bg-brand-primary/10 backdrop-blur-sm border border-brand-primary/20 rounded-full px-4 py-2 mb-6">
        <Icon name="ph:flow-arrow" class="w-5 h-5 text-brand-primary" />
        <span class="text-brand-primary font-semibold text-sm uppercase tracking-wider">{tagline}</span>
      </div>
      
      <h2 class="text-4xl lg:text-6xl font-bold text-gray-900 leading-tight mb-6">
        {title}
      </h2>
      
      <p class="text-xl text-gray-600 leading-relaxed">
        {description}
      </p>
    </div>

    <!-- Process Steps -->
    <div class="grid lg:grid-cols-3 gap-8 mb-16">
      {steps.map((step, index) => (
        <div class="relative group" data-aos="fade-up" data-aos-delay={index * 100}>
          <!-- Connection Line (hidden on mobile) -->
          {index < steps.length - 1 && (
            <div class="hidden lg:block absolute top-16 -right-4 w-8 h-0.5 bg-gradient-to-r from-brand-primary to-transparent z-10"></div>
          )}
          
          <!-- Step Card -->
          <div class="bg-white rounded-3xl shadow-2xl border border-gray-100 p-8 hover:shadow-3xl transition-all duration-500 h-full group-hover:scale-105">
            <div class="space-y-6">
              <!-- Step Number & Icon -->
              <div class="flex items-center gap-4">
                <div class="w-16 h-16 bg-gradient-to-br from-brand-primary to-brand-primary-dark rounded-2xl flex items-center justify-center text-white font-bold text-xl shadow-xl">
                  {step.number}
                </div>
                {step.icon && (
                  <div class="w-12 h-12 bg-gradient-to-br from-brand-secondary/20 to-brand-secondary/30 rounded-xl flex items-center justify-center border border-brand-secondary/30">
                    <Icon name={step.icon} class="w-6 h-6 text-brand-secondary" />
                  </div>
                )}
              </div>
              
              <!-- Content -->
              <div class="space-y-4">
                <h3 class="text-2xl font-bold text-gray-900 group-hover:text-brand-primary transition-colors duration-300">
                  {step.title}
                </h3>
                <p class="text-gray-600 leading-relaxed">
                  {step.description}
                </p>
              </div>

              <!-- Progress indicator -->
              <div class="flex items-center gap-2 pt-4 border-t border-gray-100">
                <div class="w-2 h-2 bg-brand-primary rounded-full"></div>
                <div class="text-sm text-gray-500 font-medium">Paso {step.number}</div>
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- Bottom CTA Section -->
    <div class="bg-gradient-to-r from-slate-900 to-slate-800 rounded-3xl shadow-2xl relative overflow-hidden" data-aos="fade-up" data-aos-delay="400">
      <!-- Background decoration -->
      <div class="absolute inset-0">
        <div class="absolute top-0 right-0 w-40 h-40 bg-brand-primary/10 rounded-full blur-3xl"></div>
        <div class="absolute bottom-0 left-0 w-32 h-32 bg-brand-secondary/10 rounded-full blur-2xl"></div>
      </div>

      <div class="relative z-10 p-8 lg:p-12">
        <div class="grid lg:grid-cols-2 gap-8 items-center">
          <!-- Left Content -->
          <div class="space-y-6">
            <div class="inline-flex items-center gap-2 bg-white/20 backdrop-blur-sm border border-white/30 rounded-full px-4 py-2">
              <Icon name="ph:rocket-launch" class="w-5 h-5 text-white" />
              <span class="text-white font-semibold text-sm">Proceso Garantizado</span>
            </div>

            <h3 class="text-3xl lg:text-4xl font-bold text-white leading-tight">
              ¿Listo para comenzar tu proyecto?
            </h3>
            
            <p class="text-gray-300 leading-relaxed text-lg">
              Nuestro proceso está diseñado para asegurar la máxima calidad y tu completa satisfacción 
              en cada etapa del proyecto.
            </p>
            
            <!-- Process Highlights -->
            <div class="grid grid-cols-2 gap-4">
              <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-4 text-center border border-white/20">
                <div class="w-12 h-12 bg-gradient-to-br from-brand-secondary to-brand-secondary-dark rounded-xl flex items-center justify-center mx-auto mb-3">
                  <Icon name="ph:clock" class="w-6 h-6 text-white" />
                </div>
                <div class="text-white font-semibold">Puntualidad</div>
                <div class="text-gray-300 text-sm">Garantizada</div>
              </div>
              
              <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-4 text-center border border-white/20">
                <div class="w-12 h-12 bg-gradient-to-br from-brand-primary to-brand-primary-dark rounded-xl flex items-center justify-center mx-auto mb-3">
                  <Icon name="ph:shield-check" class="w-6 h-6 text-white" />
                </div>
                <div class="text-white font-semibold">Calidad</div>
                <div class="text-gray-300 text-sm">Certificada</div>
              </div>
            </div>
          </div>

          <!-- Right Actions -->
          <div class="text-center lg:text-right space-y-6">
            <div class="space-y-3">
              <div class="inline-flex items-center gap-2 bg-white/20 backdrop-blur-sm border border-white/30 rounded-full px-4 py-2">
                <Icon name="ph:eye" class="w-4 h-4 text-white" />
                <span class="text-white font-semibold text-sm">Proceso Transparente</span>
              </div>
              <div class="text-white text-xl font-semibold">Seguimiento en tiempo real</div>
            </div>
            
            <div class="flex flex-col gap-4 lg:items-end">
              <Button 
                variant="primary"
                size="lg"
                iconPosition="left"
                icon="ph:heart"
                animation="hover"
                href="/contacto"
              >
                Iniciar Proyecto
              </Button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
